using UnityEngine;

namespace __Scripts.Quests.QuestRewardScripts
{
    public class DarkForest : MonoBehaviour
    {
        // Start is called before the first frame update
        private void Start()
        {
        }

        // Update is called once per frame
        private void Update()
        {
        }
    }
}